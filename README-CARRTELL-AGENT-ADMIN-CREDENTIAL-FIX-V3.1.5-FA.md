# اصلاح ورود واقعی ادمین در Carrtell Agent V3.1.5

ایجنت دیگر اطلاعات فرضی `admin / admin` را امتحان نمی‌کند.

هنگام اجرای `agent-category.ps1`:

1. نام کاربری واقعی ادمین پرسیده می‌شود.
2. رمز واقعی ادمین به‌شکل مخفی دریافت می‌شود.
3. اطلاعات فقط در حافظه همان PowerShell به Playwright منتقل می‌شود.
4. بعد از پایان موفق، رمز از متغیر محیطی همان پردازش پاک می‌شود.

SQL، Build جداگانه دیتابیس و Edge Function لازم ندارد.

```powershell
.\agent-category.ps1
```
