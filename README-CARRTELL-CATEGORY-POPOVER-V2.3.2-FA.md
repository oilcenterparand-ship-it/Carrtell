# Hotfix کادر شناور دسته‌بندی V2.3.2

- منوی دسته‌بندی دسکتاپ دیگر تمام صفحه را نمی‌پوشاند.
- صفحه فروشگاه پشت و پایین منو قابل مشاهده می‌ماند.
- ارتفاع Mega Menu محدود شده و با کلیک بیرون بسته می‌شود.
- نمایش مرحله‌ای تمام‌صفحه موبایل بدون تغییر باقی مانده است.
- SQL جدید لازم نیست.

پس از Extract در ریشه پروژه:

```powershell
.\agent-category.ps1
```
