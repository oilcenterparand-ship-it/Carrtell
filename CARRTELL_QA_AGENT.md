# Carrtell QA Agent

این Agent نگهبان کیفیت Carrtell است. هیچ پچ یا تغییر UI/UX نباید «تمام‌شده» محسوب شود مگر اینکه تست‌های این پکیج Pass شوند.

## قوانین غیرقابل مذاکره

1. دکمه منوی همبرگری فقط منوی سایت را باز/بسته می‌کند و هرگز نباید Login/Register را باز کند.
2. ورود/ثبت‌نام فقط از دکمه حساب/پروفایل یا مرحله احراز هویت رزرو/Checkout فراخوانی می‌شود.
3. Bottom Navigation موبایل:
   - خانه -> /
   - فروشگاه -> /shop
   - رزرو سرویس -> /book
   - حساب -> /dashboard یا مسیر ورود در صورت نیاز
   - کلیک روی تب نباید صفحه را ناخواسته به انتهای صفحه ببرد.
4. رزرو سرویس باید Wizard مرحله‌ای باشد و کاربر را مرحله‌به‌مرحله جلو ببرد:
   - شماره موبایل / OTP در صورت نیاز
   - انتخاب یا تأیید خودرو
   - نوع سرویس
   - اطلاعات لازم سرویس/کارکرد
   - آدرس
   - تاریخ
   - ساعت
   - پیشنهاد محصولات سازگار و موجود
   - خلاصه سفارش
   - پرداخت/ثبت
5. پیشنهاد محصول فقط باید با خودروی انتخاب‌شده سازگار و موجود باشد.
6. تغییرات موبایل باید حداقل روی viewport آیفون و Android بررسی شوند.
7. خطای console، صفحه سفید، route اشتباه، overlay مزاحم و کنترل غیرقابل کلیک = Fail.
8. هر باگی که یک‌بار گزارش شد باید Regression Test داشته باشد تا دوباره برنگردد.

## Definition of Done

هر تغییر فقط وقتی Done است که:
- npm run typecheck
- npm run build
- npm run qa:e2e
همگی Pass شوند.

اگر تست Fail شد Agent باید:
1. نام سناریوی Fail شده را اعلام کند.
2. screenshot و trace ذخیره کند.
3. تغییر را تحویل‌شده اعلام نکند.
4. بعد از اصلاح، کل Smoke Suite را دوباره اجرا کند.
