# Hotfix ایجنت دسته‌بندی V2.3.1

این Hotfix فقط Locator تست را دقیق می‌کند تا دکمه «دسته‌بندی محصولات» داخل منوی همبرگری را با دکمه هم‌نام نوار فروشگاه اشتباه نگیرد.

- تغییر دیتابیس و SQL ندارد.
- منطق و ظاهر سایت تغییر نمی‌کند.
- پس از جایگزینی فایل‌ها اجرا کنید:

```powershell
.\agent-category.ps1
```
