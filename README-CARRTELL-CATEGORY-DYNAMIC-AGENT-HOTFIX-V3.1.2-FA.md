# Hotfix ایجنت Dynamic دسته‌بندی V3.1.2

تست‌ها دیگر به نام یا slug ثابت دسته‌ها وابسته نیستند. ایجنت ساختار زنده دسته‌بندی را می‌خواند، اولین مادر دارای زیرشاخه را انتخاب می‌کند و سطح بعد را بررسی می‌کند؛ بنابراین تغییر نام دسته‌ها از پنل ادمین باعث شکست تست نخواهد شد.

- SQL ندارد.
- ظاهر سایت تغییر نمی‌کند.
- اجرا: `.\agent-category.ps1`
