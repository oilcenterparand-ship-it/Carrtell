# Carrtell Cart + Orders Patch

This patch adds:
- Real cart page at `/cart`
- Cart persistence with localStorage
- Order registration
- Order items table
- Automatic stock reduction after order submission
- Admin orders page at `/admin/orders`

Run `database/cart_orders.sql` in Supabase SQL Editor, then replace the `src` files.
