import { useEffect, useMemo, useState } from 'react';
import { Link, useNavigate } from 'react-router-dom';
import { ArrowLeft, CalendarDays, Clock3, LogIn, ShieldCheck, Trash2 } from 'lucide-react';
import { CheckCircle2, CreditCard, MapPin, PackageCheck, ShoppingCart, Truck, Wrench } from 'lucide-react';
import MapLocationPicker from '../components/MapLocationPicker';
import { supabase } from '../lib/supabase';
import { useAuth } from '../auth/AuthProvider';
import { emitAuthChanged, normalizeDigits } from '../auth/authApi';
import { IRAN_PROVINCES, getProvinceCounties } from '../data/iranLocations';
import { clearLegacyPersistentCart } from '../lib/cart';
import DiscountCodeBox from '../components/discounts/DiscountCodeBox';
import type { DiscountCheckResult } from '../services/discountsClientApi';

type CartItem = {
  id: string;
  product_id?: string;
  name?: string;
  title?: string;
  image?: string;
  image_url?: string;
  price?: number;
  sale_price?: number;
  quantity?: number;
  qty?: number;
  brand_name?: string;
  category_name?: string;
};

type CustomerAddress = {
  id: string;
  title?: string | null;
  city?: string | null;
  district?: string | null;
  neighborhood?: string | null;
  street?: string | null;
  plaque?: string | null;
  unit?: string | null;
  latitude?: number | null;
  longitude?: number | null;
  full_address?: string | null;
};

type SelectedCar = {
  id?: string;
  car_id?: string;
  name?: string;
  title?: string;
  brand?: string;
  model?: string;
  year?: string | number;
};


type StepKey = 'cart' | 'info' | 'address' | 'payment' | 'done';

type AddressFieldErrors = {
  province?: string;
  county?: string;
  street?: string;
  postalCode?: string;
};

const steps: { key: StepKey; label: string; icon: JSX.Element }[] = [
  { key: 'cart', label: 'Ø³Ø¨Ø¯ Ø®Ø±ÛŒØ¯', icon: <ShoppingCart className="h-4 w-4" /> },
  { key: 'info', label: 'Ø§Ø·Ù„Ø§Ø¹Ø§Øª', icon: <PackageCheck className="h-4 w-4" /> },
  { key: 'address', label: 'Ø¢Ø¯Ø±Ø³', icon: <MapPin className="h-4 w-4" /> },
  { key: 'payment', label: 'Ù¾Ø±Ø¯Ø§Ø®Øª', icon: <CreditCard className="h-4 w-4" /> },
  { key: 'done', label: 'ØªÚ©Ù…ÛŒÙ„', icon: <CheckCircle2 className="h-4 w-4" /> },
];

const inputClass = 'w-full rounded-2xl border border-white/10 bg-slate-950/70 px-4 py-3 text-sm text-white outline-none transition placeholder:text-slate-500 focus:border-amber-400/70 focus:ring-2 focus:ring-amber-400/20';
const selectClass = `${inputClass} [&>option]:bg-slate-950 [&>option]:text-white`;
const cardClass = 'rounded-3xl border border-white/10 bg-slate-900/75 p-4 shadow-2xl shadow-black/20 backdrop-blur';

function toNumber(value: unknown) {
  const n = Number(value ?? 0);
  return Number.isFinite(n) ? n : 0;
}

function money(value: number) {
  return `${Math.round(value).toLocaleString('fa-IR')} ØªÙˆÙ…Ø§Ù†`;
}

function toLocalCheckoutPhone(value: unknown) {
  const digits = normalizeDigits(String(value || '')).replace(/\D/g, '');
  if (/^989\d{9}$/.test(digits)) return `0${digits.slice(2)}`;
  if (/^9\d{9}$/.test(digits)) return `0${digits}`;
  return digits;
}

const SERVICE_TIME_SLOTS = [
  { value: '08:00', label: 'Û¸ ØªØ§ Û±Û°' },
  { value: '10:00', label: 'Û±Û° ØªØ§ Û±Û²' },
  { value: '12:00', label: 'Û±Û² ØªØ§ Û±Û´' },
  { value: '14:00', label: 'Û±Û´ ØªØ§ Û±Û¶' },
  { value: '16:00', label: 'Û±Û¶ ØªØ§ Û±Û¸' },
  { value: '18:00', label: 'Û±Û¸ ØªØ§ Û²Û°' },
];

function toLocalIsoDate(date: Date) {
  const year = date.getFullYear();
  const month = String(date.getMonth() + 1).padStart(2, '0');
  const day = String(date.getDate()).padStart(2, '0');
  return `${year}-${month}-${day}`;
}

function persianServiceDate(date: Date) {
  return new Intl.DateTimeFormat('fa-IR-u-ca-persian', {
    weekday: 'short',
    day: 'numeric',
    month: 'long',
  }).format(date);
}

function getItemId(item: CartItem) {
  return String(item.product_id || item.id);
}

function getItemName(item: CartItem) {
  return item.name || item.title || 'Ù…Ø­ØµÙˆÙ„ Ú©Ø§Ø±ØªÙ„';
}

function getItemPrice(item: CartItem) {
  return toNumber(item.sale_price || item.price);
}

function getItemQty(item: CartItem) {
  return Math.max(1, toNumber(item.quantity || item.qty || 1));
}

function readJson<T>(keys: string[], fallback: T): T {
  for (const key of keys) {
    try {
      const raw = localStorage.getItem(key);
      if (!raw) continue;
      return JSON.parse(raw) as T;
    } catch {
      // ignore old invalid values
    }
  }
  return fallback;
}

function normalizeStoredCart(value: unknown): CartItem[] {
  if (Array.isArray(value)) return value as CartItem[];
  if (!value || typeof value !== 'object') return [];

  return Object.values(value as Record<string, any>)
    .map((entry: any) => {
      if (entry?.product) {
        return {
          ...entry.product,
          id: entry.product.id,
          product_id: entry.product.id,
          quantity: Number(entry.quantity || 1),
          qty: Number(entry.quantity || 1),
        } as CartItem;
      }
      return entry as CartItem;
    })
    .filter((item: CartItem) => Boolean(item?.id || item?.product_id));
}

function readCartItems(): CartItem[] {
  try {
    const raw = sessionStorage.getItem('carrtell_cart_v1');
    return raw ? normalizeStoredCart(JSON.parse(raw)) : [];
  } catch {
    return [];
  }
}

function writeCart(items: CartItem[]) {
  const normalized = items.map((item) => ({ ...item, quantity: getItemQty(item), qty: getItemQty(item) }));
  const cartMap = Object.fromEntries(
    normalized.map((item) => {
      const id = getItemId(item);
      return [id, { product: { ...item, id }, quantity: getItemQty(item) }];
    }),
  );

  sessionStorage.setItem('carrtell_cart_v1', JSON.stringify(cartMap));
  window.dispatchEvent(new CustomEvent('carrtell-cart-updated', { detail: cartMap }));
  window.dispatchEvent(new Event('cart:updated'));
}

function getCarTitle(car: SelectedCar | null) {
  if (!car) return '';
  return [car.brand, car.name || car.title || car.model, car.year].filter(Boolean).join(' ');
}

function addressText(address?: CustomerAddress | null) {
  if (!address) return '';
  return address.full_address || [address.city, address.district || address.neighborhood, address.street, address.plaque ? `Ù¾Ù„Ø§Ú© ${address.plaque}` : '', address.unit ? `ÙˆØ§Ø­Ø¯ ${address.unit}` : ''].filter(Boolean).join('ØŒ ');
}

export default function CartPage() {
  const navigate = useNavigate();
  const { user: authUser, isAuthenticated, loading: authLoading, refreshAuth } = useAuth();
  const [step, setStep] = useState<StepKey>('cart');
  const [items, setItems] = useState<CartItem[]>([]);
  const [addresses, setAddresses] = useState<CustomerAddress[]>([]);
  const [selectedAddressId, setSelectedAddressId] = useState('');
  const [showNewAddressForm, setShowNewAddressForm] = useState(true);
  const [addressFieldErrors, setAddressFieldErrors] = useState<AddressFieldErrors>({});
  const [selectedCar, setSelectedCar] = useState<SelectedCar | null>(null);
  const [loading, setLoading] = useState(false);
  const [message, setMessage] = useState('');
  const [createdOrderId, setCreatedOrderId] = useState('');
  const [customer, setCustomer] = useState({ full_name: '', phone: '', note: '' });
  const [newAddress, setNewAddress] = useState({ province: '', county: '', neighborhood: '', street: '', plaque: '', unit: '', postal_code: '', latitude: '', longitude: '' });
  const [saveAddressForLater, setSaveAddressForLater] = useState(false);
  const [addressTitle, setAddressTitle] = useState('');
  const [savingAddress, setSavingAddress] = useState(false);
  const [deliveryMode, setDeliveryMode] = useState<'delivery' | 'service'>('delivery');
  const [shippingMethod, setShippingMethod] = useState<'standard' | 'express' | 'pickup'>('standard');
  const [dispatchFee, setDispatchFee] = useState(150000);
  const [serviceLaborFee, setServiceLaborFee] = useState(200000);
  const [serviceDate, setServiceDate] = useState('');
  const [serviceTime, setServiceTime] = useState('');
  const [appliedDiscount, setAppliedDiscount] = useState<DiscountCheckResult | null>(null);
  const availableServiceDates = useMemo(() => Array.from({ length: 14 }, (_, index) => {
    const date = new Date();
    date.setHours(12, 0, 0, 0);
    date.setDate(date.getDate() + index);
    return { value: toLocalIsoDate(date), label: persianServiceDate(date), isToday: index === 0 };
  }), []);

  const subtotal = useMemo(() => items.reduce((sum, item) => sum + getItemPrice(item) * getItemQty(item), 0), [items]);
  const serviceFee = deliveryMode === 'service' ? dispatchFee + serviceLaborFee : 0;
  const shippingFee = deliveryMode === 'delivery'
    ? shippingMethod === 'pickup'
      ? 0
      : shippingMethod === 'express'
        ? 220000
        : subtotal >= 3000000
          ? 0
          : 120000
    : 0;
  const discountAmount = Math.min(Number(appliedDiscount?.amount || 0), subtotal);
  const total = Math.max(0, subtotal + serviceFee + shippingFee - discountAmount);
  const freeShippingThreshold = 3000000;
  const freeShippingRemaining = Math.max(0, freeShippingThreshold - subtotal);
  const freeShippingProgress = Math.min(100, (subtotal / freeShippingThreshold) * 100);
  const selectedAddress = addresses.find((a) => a.id === selectedAddressId) || null;
  const carTitle = getCarTitle(selectedCar);
  const customerInfoValid = Boolean(customer.full_name.trim() && /^09\d{9}$/.test(toLocalCheckoutPhone(customer.phone)));
  const postalRequired = false;
  const postalCodeValid = !newAddress.postal_code.trim() || /^\d{10}$/.test(newAddress.postal_code.replace(/\D/g, ''));
  const addressReady = shippingMethod === 'pickup'
    ? true
    : Boolean((selectedAddressId && !showNewAddressForm) || (newAddress.province.trim() && newAddress.county.trim() && newAddress.street.trim() && postalCodeValid));
  const serviceScheduleReady = deliveryMode !== 'service' || Boolean(serviceDate && serviceTime);


  useEffect(() => {
    clearLegacyPersistentCart();
    localStorage.removeItem('carrtell_saved_addresses');
    setItems(readCartItems());
    setSelectedCar(readJson<SelectedCar | null>(['carrtell_selected_car', 'selected_car', 'my_selected_car'], null));
    // Ø¢Ø¯Ø±Ø³ Ø¹Ù…ÙˆÙ…ÛŒ Ù‚Ø¯ÛŒÙ…ÛŒ Ø¹Ù…Ø¯Ø§Ù‹ Ø®ÙˆØ§Ù†Ø¯Ù‡ Ù†Ù…ÛŒâ€ŒØ´ÙˆØ¯Ø› Ø¢Ø¯Ø±Ø³ Ø¨Ø§ÛŒØ¯ Ø¨Ø¹Ø¯ Ø§Ø² ØªØ´Ø®ÛŒØµ Ú©Ø§Ø±Ø¨Ø± Ø¨Ø§Ø±Ú¯Ø°Ø§Ø±ÛŒ Ø´ÙˆØ¯.
    setAddresses([]);
    setSelectedAddressId('');
    setShowNewAddressForm(true);

    try {
      const rawDraft = sessionStorage.getItem('carrtell_checkout_draft');
      if (rawDraft) {
        const draft = JSON.parse(rawDraft);
        if (draft.customer) setCustomer({ ...draft.customer, phone: toLocalCheckoutPhone(draft.customer.phone) });
        if (draft.newAddress) setNewAddress(draft.newAddress);
        if (draft.deliveryMode) setDeliveryMode(draft.deliveryMode);
        if (draft.shippingMethod) setShippingMethod(draft.shippingMethod);
        if (draft.serviceDate) setServiceDate(draft.serviceDate);
        if (draft.serviceTime) setServiceTime(draft.serviceTime);
        if (draft.selectedAddressId) { setSelectedAddressId(draft.selectedAddressId); setShowNewAddressForm(false); }
      }
    } catch { /* invalid old checkout draft */ }

    supabase.auth.getUser().then(async ({ data }) => {
      const user = data.user;
      if (!user) return;

      const metadata = user.user_metadata || {};
      setCustomer((prev) => ({
        ...prev,
        full_name: metadata.full_name || metadata.name || prev.full_name,
        phone: toLocalCheckoutPhone(metadata.phone || user.phone || prev.phone),
      }));

      const { data: profile } = await supabase
        .from('profiles')
        .select('full_name, phone')
        .eq('id', user.id)
        .maybeSingle();

      if (profile) {
        setCustomer((prev) => ({
          ...prev,
          full_name: profile.full_name || prev.full_name,
          phone: toLocalCheckoutPhone(profile.phone || prev.phone),
        }));
      }

      const addressQueries = [
        supabase.from('customer_addresses').select('*').eq('user_id', user.id).order('created_at', { ascending: false }),
        supabase.from('addresses').select('*').eq('user_id', user.id).order('created_at', { ascending: false }),
      ];

      for (const query of addressQueries) {
        const { data: rows, error } = await query;
        if (!error && rows && rows.length) {
          setAddresses(rows as CustomerAddress[]);
          setSelectedAddressId(String(rows[0].id));
          break;
        }
      }
    });
  }, []);

  useEffect(() => {
    let cancelled = false;

    const loadAddressesForCurrentUser = async () => {
      setAddresses([]);
      setSelectedAddressId('');
      setShowNewAddressForm(true);

      if (!isAuthenticated) return;

      const cleanPhone = String(authUser?.phone || customer.phone || '').replace(/\s|-/g, '').trim();
      const userId = authUser?.id || null;
      const identityKey = userId || cleanPhone;
      if (!identityKey) return;

      const queries = userId
        ? [
            supabase.from('customer_addresses').select('*').eq('user_id', userId).order('created_at', { ascending: false }),
            supabase.from('addresses').select('*').eq('user_id', userId).order('created_at', { ascending: false }),
          ]
        : [
            supabase.from('customer_addresses').select('*').eq('customer_phone', cleanPhone).order('created_at', { ascending: false }),
            supabase.from('addresses').select('*').eq('customer_phone', cleanPhone).order('created_at', { ascending: false }),
          ];

      for (const query of queries) {
        const { data: rows, error } = await query;
        if (!error && rows && rows.length) {
          if (cancelled) return;
          const ownedRows = (rows as CustomerAddress[]).filter((row: any) => {
            if (userId && row.user_id) return row.user_id === userId;
            return String(row.customer_phone || '').replace(/\s|-/g, '').trim() === cleanPhone;
          });
          if (ownedRows.length) {
            setAddresses(ownedRows);
            setSelectedAddressId(String(ownedRows[0].id || ''));
            setShowNewAddressForm(false);
            return;
          }
        }
      }

      // fallback Ù…Ø­Ù„ÛŒ Ù†ÛŒØ² Ø¨Ø±Ø§ÛŒ Ù‡Ø± Ú©Ø§Ø±Ø¨Ø± Ú©Ù„ÛŒØ¯ Ø¬Ø¯Ø§ Ø¯Ø§Ø±Ø¯.
      try {
        const localKey = `carrtell_saved_addresses:${identityKey}`;
        const localRows = JSON.parse(localStorage.getItem(localKey) || '[]') as CustomerAddress[];
        if (!cancelled && localRows.length) {
          setAddresses(localRows);
          setSelectedAddressId(String(localRows[0].id || ''));
          setShowNewAddressForm(false);
        }
      } catch {
        // Ø¢Ø¯Ø±Ø³ Ù…Ø­Ù„ÛŒ Ø®Ø±Ø§Ø¨ Ù†Ø§Ø¯ÛŒØ¯Ù‡ Ú¯Ø±ÙØªÙ‡ Ù…ÛŒâ€ŒØ´ÙˆØ¯.
      }
    };

    void loadAddressesForCurrentUser();
    return () => { cancelled = true; };
  }, [isAuthenticated, authUser?.id, authUser?.phone]);

  useEffect(() => {
    const loadServiceFees = async () => {
      const { data, error } = await supabase
        .from('system_settings')
        .select('group_key, setting_key, setting_value')
        .in('setting_key', ['base_dispatch_fee', 'base_service_fee', 'service_fee']);

      if (error || !data) return;
      const dispatch = data.find((row: any) => row.setting_key === 'base_dispatch_fee');
      const labor = data.find((row: any) => row.setting_key === 'base_service_fee' || row.setting_key === 'service_fee');
      if (dispatch && toNumber(dispatch.setting_value) > 0) setDispatchFee(toNumber(dispatch.setting_value));
      if (labor && toNumber(labor.setting_value) > 0) setServiceLaborFee(toNumber(labor.setting_value));
    };
    void loadServiceFees();
  }, []);

  useEffect(() => {
    if (authUser) {
      setCustomer((prev) => ({
        ...prev,
        full_name: authUser.fullName || prev.full_name,
        phone: toLocalCheckoutPhone(authUser.phone || prev.phone),
      }));
    }
  }, [authUser]);

  useEffect(() => {
    if (authLoading) return;
    if (!items.length && step !== 'cart') { setStep('cart'); return; }
    const resume = sessionStorage.getItem('carrtell_checkout_resume');
    if (isAuthenticated && resume === 'payment' && items.length) {
      setStep('payment');
      sessionStorage.removeItem('carrtell_checkout_resume');
    }
  }, [authLoading, isAuthenticated, items.length, step]);

  useEffect(() => {
    const sync = () => setItems(readCartItems());
    window.addEventListener('carrtell-cart-updated', sync as EventListener);
    window.addEventListener('storage', sync);
    return () => {
      window.removeEventListener('carrtell-cart-updated', sync as EventListener);
      window.removeEventListener('storage', sync);
    };
  }, []);


  function updateQty(id: string, qty: number) {
    const next = items
      .map((item) => (getItemId(item) === id ? { ...item, quantity: Math.max(1, qty), qty: Math.max(1, qty) } : item))
      .filter((item) => getItemQty(item) > 0);
    setItems(next);
    writeCart(next);
  }

  function removeItem(id: string) {
    const next = items.filter((item) => getItemId(item) !== id);
    setItems(next);
    writeCart(next);
  }

  function beginCheckout() {
    setMessage('');
    if (!items.length) {
      setMessage('Ø³Ø¨Ø¯ Ø®Ø±ÛŒØ¯ Ø®Ø§Ù„ÛŒ Ø§Ø³Øª.');
      return;
    }
    setStep('info');
  }

  async function saveCustomerAndContinue() {
    setMessage('');
    if (!customer.full_name.trim()) {
      setMessage('Ù†Ø§Ù… Ùˆ Ù†Ø§Ù… Ø®Ø§Ù†ÙˆØ§Ø¯Ú¯ÛŒ Ø±Ø§ ÙˆØ§Ø±Ø¯ Ú©Ù†.');
      return;
    }
    const normalizedPhone = toLocalCheckoutPhone(customer.phone);
    if (!/^09\d{9}$/.test(normalizedPhone)) {
      setMessage('Ø´Ù…Ø§Ø±Ù‡ Ù…ÙˆØ¨Ø§ÛŒÙ„ Ù…Ø¹ØªØ¨Ø± ÙˆØ§Ø±Ø¯ Ú©Ù†Ø› Ù…Ø§Ù†Ù†Ø¯ 09123456789.');
      return;
    }

    localStorage.setItem('carrtell_customer_profile', JSON.stringify({
      phone: normalizedPhone,
      fullName: customer.full_name.trim(),
    }));
    localStorage.setItem('carrtell_user_role', authUser?.role || 'customer');
    emitAuthChanged();

    const { data } = await supabase.auth.getUser();
    if (data.user?.id) {
      await supabase.from('profiles').upsert({
        id: data.user.id,
        full_name: customer.full_name.trim(),
        phone: normalizedPhone,
        updated_at: new Date().toISOString(),
      }, { onConflict: 'id' });
      await refreshAuth();
    }

    setStep('address');
  }

  function hasAddress() {
    if (selectedAddressId && !showNewAddressForm) return true;
    return Boolean(newAddress.province.trim() && newAddress.county.trim() && newAddress.street.trim());
  }

  function validateNewAddress() {
    const errors: AddressFieldErrors = {};
    if (!newAddress.province.trim()) errors.province = 'Ø§Ø³ØªØ§Ù† Ø±Ø§ Ø§Ù†ØªØ®Ø§Ø¨ Ú©Ù†.';
    if (!newAddress.county.trim()) errors.county = 'Ø´Ù‡Ø±Ø³ØªØ§Ù† Ø±Ø§ Ø§Ù†ØªØ®Ø§Ø¨ Ú©Ù†.';
    if (!newAddress.street.trim()) errors.street = 'Ø¢Ø¯Ø±Ø³ Ø¯Ù‚ÛŒÙ‚ Ø±Ø§ ÙˆØ§Ø±Ø¯ Ú©Ù†.';
    if (newAddress.postal_code.trim() && !/^\d{10}$/.test(newAddress.postal_code.replace(/\D/g, ''))) errors.postalCode = 'Ø¯Ø± ØµÙˆØ±Øª ÙˆØ§Ø±Ø¯Ú©Ø±Ø¯Ù† Ú©Ø¯Ù¾Ø³ØªÛŒØŒ Ø¨Ø§ÛŒØ¯ Û±Û° Ø±Ù‚Ù… Ø¨Ø§Ø´Ø¯.';
    setAddressFieldErrors(errors);
    return Object.keys(errors).length === 0;
  }

  function canGoPayment() {
    if (deliveryMode === 'service' && !serviceScheduleReady) {
      setMessage('ØªØ§Ø±ÛŒØ® Ùˆ Ø³Ø§Ø¹Øª Ø¯Ø±ÛŒØ§ÙØª Ø³Ø±ÙˆÛŒØ³ Ø±Ø§ Ø§Ù†ØªØ®Ø§Ø¨ Ú©Ù†.');
      return false;
    }
    if (selectedAddressId && !showNewAddressForm) {
      setAddressFieldErrors({});
      return true;
    }
    return validateNewAddress();
  }

  async function saveAddressIfRequested() {
    if (!saveAddressForLater || selectedAddressId) return true;
    if (!addressTitle.trim()) {
      setMessage('Ø¨Ø±Ø§ÛŒ Ø°Ø®ÛŒØ±Ù‡ Ø¢Ø¯Ø±Ø³ØŒ ÛŒÚ© Ù†Ø§Ù… Ù…Ø«Ù„ Ø®Ø§Ù†Ù‡ ÛŒØ§ Ù…Ø­Ù„ Ú©Ø§Ø± ÙˆØ§Ø±Ø¯ Ú©Ù†.');
      return false;
    }
    if (!hasAddress()) {
      validateNewAddress();
      return false;
    }

    setSavingAddress(true);
    try {
      const { data: authData } = await supabase.auth.getUser();
      const payload = {
        user_id: authData.user?.id || authUser?.id || null,
        customer_phone: customer.phone.trim(),
        title: addressTitle.trim(),
        province: newAddress.province,
        city: newAddress.county,
        district: newAddress.neighborhood || null,
        neighborhood: newAddress.neighborhood || null,
        street: newAddress.street,
        plaque: newAddress.plaque,
        unit: newAddress.unit || null,
        postal_code: newAddress.postal_code || null,
        latitude: newAddress.latitude ? Number(newAddress.latitude) : null,
        longitude: newAddress.longitude ? Number(newAddress.longitude) : null,
        full_address: [newAddress.province, newAddress.county, newAddress.neighborhood, newAddress.street, newAddress.plaque ? `Ù¾Ù„Ø§Ú© ${newAddress.plaque}` : '', newAddress.unit ? `ÙˆØ§Ø­Ø¯ ${newAddress.unit}` : ''].filter(Boolean).join('ØŒ '),
      };

      let saved: any = null;
      for (const table of ['customer_addresses', 'addresses']) {
        const { data, error } = await supabase.from(table).insert(payload).select('*').single();
        if (!error && data) { saved = data; break; }
      }
      if (!saved) {
        saved = { ...payload, id: crypto.randomUUID() };
        const cleanPhone = customer.phone.replace(/\s|-/g, '').trim();
        const identityKey = authData.user?.id || authUser?.id || cleanPhone;
        const localKey = `carrtell_saved_addresses:${identityKey}`;
        const local = JSON.parse(localStorage.getItem(localKey) || '[]');
        localStorage.setItem(localKey, JSON.stringify([saved, ...local]));
      }
      setAddresses((prev) => [saved as CustomerAddress, ...prev]);
      setSelectedAddressId(String(saved.id));
      return true;
    } catch {
      setMessage('Ø°Ø®ÛŒØ±Ù‡ Ø¢Ø¯Ø±Ø³ Ø§Ù†Ø¬Ø§Ù… Ù†Ø´Ø¯Ø› Ø¯ÙˆØ¨Ø§Ø±Ù‡ ØªÙ„Ø§Ø´ Ú©Ù†.');
      return false;
    } finally {
      setSavingAddress(false);
    }
  }

  async function goToPayment() {
    setMessage('');
    if (!canGoPayment()) return;
    const saved = await saveAddressIfRequested();
    if (!saved) return;

    if (!isAuthenticated) {
      sessionStorage.setItem('carrtell_checkout_draft', JSON.stringify({
        customer: { ...customer, phone: normalizeDigits(customer.phone).replace(/\D/g, '') },
        newAddress, deliveryMode, shippingMethod, serviceDate, serviceTime, selectedAddressId,
      }));
      sessionStorage.setItem('carrtell_checkout_resume', 'payment');
      navigate('/login-otp?returnTo=%2Fcart');
      return;
    }

    setStep('payment');
  }

  async function createOrder() {
    if (!isAuthenticated) {
      sessionStorage.setItem('carrtell_checkout_resume', 'payment');
      navigate('/login-otp?returnTo=%2Fcart');
      return;
    }
    if (!customerInfoValid) {
      setMessage('Ø§Ø¨ØªØ¯Ø§ Ø§Ø·Ù„Ø§Ø¹Ø§Øª Ù…Ø´ØªØ±ÛŒ Ø±Ø§ Ú©Ø§Ù…Ù„ Ú©Ù†.');
      setStep('info');
      return;
    }
    if (!canGoPayment()) {
      setStep('address');
      return;
    }
    setLoading(true);
    setMessage('');

    try {
      const { data: authData } = await supabase.auth.getUser();
      const user = authData.user;
      const carId = selectedCar?.car_id || selectedCar?.id || null;
      const orderAddress = selectedAddress
        ? addressText(selectedAddress)
        : [newAddress.province, newAddress.county, newAddress.neighborhood, newAddress.street].filter(Boolean).join('ØŒ ');

      const orderPayload = {
        user_id: user?.id || authUser?.id || null,
        customer_name: customer.full_name,
        customer_phone: customer.phone,
        status: 'pending_payment',
        payment_status: 'unpaid',
        total_amount: total,
        subtotal,
        shipping_amount: shippingFee,
        service_amount: serviceFee,
        discount_code: appliedDiscount?.code || null,
        discount_id: appliedDiscount?.discountId || null,
        discount_amount: discountAmount,
        delivery_type: deliveryMode === 'service' ? 'service' : shippingMethod,
        car_id: carId,
        car_name: carTitle || null,
        address_id: selectedAddress?.id || null,
        address_text: shippingMethod === 'pickup' ? 'ØªØ­ÙˆÛŒÙ„ Ø­Ø¶ÙˆØ±ÛŒ Ø§Ø² Ø´Ø¹Ø¨Ù‡' : orderAddress,
        latitude: selectedAddress?.latitude || (newAddress.latitude ? Number(newAddress.latitude) : null),
        longitude: selectedAddress?.longitude || (newAddress.longitude ? Number(newAddress.longitude) : null),
        customer_note: customer.note || null,
        items: deliveryMode === 'service' ? [
          ...items,
          { id: 'service-dispatch-fee', name: 'Ù‡Ø²ÛŒÙ†Ù‡ Ø§ÛŒØ§Ø¨ Ùˆ Ø°Ù‡Ø§Ø¨ Ø³Ø±ÙˆÛŒØ³ Ø¯Ø± Ù…Ø­Ù„', price: dispatchFee, quantity: 1 },
          { id: 'service-labor-fee', name: 'Ø§Ø¬Ø±Øª Ø³Ø±ÙˆÛŒØ³ Ø¯Ø± Ù…Ø­Ù„', price: serviceLaborFee, quantity: 1 },
        ] : items,
      };

      const serviceDraft = deliveryMode === 'service'
        ? {
            customer_name: customer.full_name,
            customer_phone: customer.phone,
            car_id: carId,
            car_name: carTitle || null,
            vehicle_title: carTitle || null,
            address_text: orderAddress,
            latitude: selectedAddress?.latitude || (newAddress.latitude ? Number(newAddress.latitude) : null),
            longitude: selectedAddress?.longitude || (newAddress.longitude ? Number(newAddress.longitude) : null),
            preferred_date: serviceDate,
            preferred_time: serviceTime,
            scheduled_at: `${serviceDate}T${serviceTime}:00`,
            status: 'pending_review',
            service_items: items,
            estimated_total: total,
            customer_note: customer.note || null,
          }
        : null;

      const { data: checkoutResult, error } = await supabase.rpc(
        'carrtell_create_checkout_order',
        {
          p_order: orderPayload,
          p_service: serviceDraft,
        },
      );

      if (error) throw error;

      const order =
        checkoutResult && typeof checkoutResult === 'object'
          ? checkoutResult
          : null;

      if (!order?.id) {
        throw new Error('Ø´Ù†Ø§Ø³Ù‡ Ø³ÙØ§Ø±Ø´ Ø§Ø² Ø³Ø±ÙˆØ± Ø¯Ø±ÛŒØ§ÙØª Ù†Ø´Ø¯.');
      }

      const orderId = String(order.id);

      setCreatedOrderId(orderId);
      sessionStorage.setItem('carrtell_last_order_id', orderId);
      setStep('done');
      navigate(`/payment?orderId=${encodeURIComponent(orderId)}`);
    } catch (error: any) {
      setMessage(error?.message || 'Ø«Ø¨Øª Ø³ÙØ§Ø±Ø´ Ø§Ù†Ø¬Ø§Ù… Ù†Ø´Ø¯. Ø¬Ø¯ÙˆÙ„ orders ÛŒØ§ Ø³ØªÙˆÙ†â€ŒÙ‡Ø§ÛŒ Ù¾Ø±Ø¯Ø§Ø®Øª Ø±Ø§ Ø¨Ø±Ø±Ø³ÛŒ Ú©Ù†.');
    } finally {
      setLoading(false);
    }
  }

  if (authLoading) {
    return (
      <main className="flex min-h-screen items-center justify-center bg-slate-50 px-4 text-slate-900" dir="rtl">
        <div className="rounded-3xl border border-slate-200 bg-white px-8 py-6 text-center shadow-xl">
          <p className="font-bold">Ø¯Ø± Ø­Ø§Ù„ Ø¨Ø±Ø±Ø³ÛŒ ÙˆØ¶Ø¹ÛŒØª ÙˆØ±ÙˆØ¯...</p>
        </div>
      </main>
    );
  }

  if (false && !isAuthenticated) {
    return (
      <main className="flex min-h-screen items-center justify-center bg-slate-50 px-4 text-slate-900" dir="rtl">
        <section className="w-full max-w-md rounded-[2rem] border border-slate-200 bg-white p-7 text-center shadow-2xl">
          <div className="mx-auto flex h-16 w-16 items-center justify-center rounded-2xl bg-rose-500 text-white">
            <LogIn className="h-8 w-8" />
          </div>
          <h1 className="mt-5 text-2xl font-black text-slate-950">Ø¨Ø±Ø§ÛŒ Ù…Ø´Ø§Ù‡Ø¯Ù‡ Ø³Ø¨Ø¯ Ø®Ø±ÛŒØ¯ ÙˆØ§Ø±Ø¯ Ø´Ùˆ</h1>
          <p className="mt-3 text-sm leading-7 text-slate-600">Ø³Ø¨Ø¯ Ø®Ø±ÛŒØ¯ Ùˆ Ù…Ø±Ø§Ø­Ù„ Ø«Ø¨Øª Ø³ÙØ§Ø±Ø´ ÙÙ‚Ø· Ø¨Ø¹Ø¯ Ø§Ø² ÙˆØ±ÙˆØ¯ Ø¨Ø§ Ø´Ù…Ø§Ø±Ù‡ Ù…ÙˆØ¨Ø§ÛŒÙ„ Ø¯Ø± Ø¯Ø³ØªØ±Ø³ Ø§Ø³Øª.</p>
          <button
            type="button"
            onClick={() => navigate('/login-otp?returnTo=%2Fcart')}
            className="mt-6 inline-flex w-full items-center justify-center gap-2 rounded-2xl bg-rose-500 px-5 py-3.5 font-black text-white transition hover:bg-rose-600"
          >
            <LogIn className="h-5 w-5" /> ÙˆØ±ÙˆØ¯ / Ø«Ø¨Øªâ€ŒÙ†Ø§Ù… Ø¨Ø§ Ù…ÙˆØ¨Ø§ÛŒÙ„
          </button>
        </section>
      </main>
    );
  }

  return (
    <main className="cart-page-theme min-h-screen bg-slate-50 px-4 pb-8 pt-32 text-slate-900 sm:px-6 lg:px-8 lg:pt-36" dir="rtl">
      <div className="mx-auto max-w-7xl space-y-6">
        <section className="rounded-[2rem] border border-amber-400/20 bg-gradient-to-br from-slate-900 via-slate-950 to-amber-950/25 p-5 shadow-2xl shadow-amber-950/20">
          <div className="flex flex-col gap-4 lg:flex-row lg:items-center lg:justify-between">
            <div>
              <p className="text-sm text-amber-300">ØªÚ©Ù…ÛŒÙ„ Ø®Ø±ÛŒØ¯ Carrtell</p>
              <h1 className="mt-1 text-2xl font-black sm:text-3xl">Ø³Ø¨Ø¯ Ø®Ø±ÛŒØ¯ Ùˆ Ø«Ø¨Øª Ø³ÙØ§Ø±Ø´</h1>
              <p className="mt-2 text-sm text-slate-300">Ù¾Ø³ Ø§Ø² ÙˆØ±ÙˆØ¯ØŒ Ø§Ø·Ù„Ø§Ø¹Ø§Øª Ù…Ø´ØªØ±ÛŒØŒ Ø¢Ø¯Ø±Ø³ØŒ Ø±ÙˆØ´ Ø¯Ø±ÛŒØ§ÙØªØŒ Ù¾Ø±Ø¯Ø§Ø®Øª Ùˆ ÙØ§Ú©ØªÙˆØ± Ø±Ø§ Ù…Ø±Ø­Ù„Ù‡â€ŒØ¨Ù‡â€ŒÙ…Ø±Ø­Ù„Ù‡ ØªÚ©Ù…ÛŒÙ„ Ú©Ù†.</p>
            </div>
            {step === 'cart' && (carTitle ? (
              <div className="rounded-2xl border border-amber-300/30 bg-amber-400/10 px-4 py-3 text-sm text-amber-100">
                Ø³ÙØ§Ø±Ø´ Ø¨Ø±Ø§ÛŒ Ø®ÙˆØ¯Ø±ÙˆÛŒ Ø´Ù…Ø§: <b>{carTitle}</b>
              </div>
            ) : (
              <Link to="/profile" className="rounded-2xl border border-white/10 bg-white/5 px-4 py-3 text-sm text-slate-200 hover:bg-white/10">
                Ø®ÙˆØ¯Ø±Ùˆ Ø§Ù†ØªØ®Ø§Ø¨ Ù†Ø´Ø¯Ù‡Ø› Ø§Ø² Ù¾Ø±ÙˆÙØ§ÛŒÙ„ ÛŒØ§ Ù‡Ø¯Ø± Ø®ÙˆØ¯Ø±Ùˆ Ø±Ø§ Ø§Ù†ØªØ®Ø§Ø¨ Ú©Ù†.
              </Link>
            ))}
          </div>

          <div className="mt-6 grid grid-cols-2 gap-2 md:grid-cols-5">
            {steps.map((item, index) => {
              const activeIndex = steps.findIndex((s) => s.key === step);
              const isActive = item.key === step;
              const done = index < activeIndex;
              const canGoBack = index < activeIndex;
              const disabled = !canGoBack;
              return (
                <button
                  key={item.key}
                  type="button"
                  disabled={disabled}
                  onClick={() => {
                    if (canGoBack) {
                      setMessage('');
                      setStep(item.key);
                    }
                  }}
                  title={
                    isActive
                      ? 'Ù…Ø±Ø­Ù„Ù‡ ÙØ¹Ù„ÛŒ'
                      : canGoBack
                        ? 'Ø¨Ø§Ø²Ú¯Ø´Øª Ø¨Ù‡ Ø§ÛŒÙ† Ù…Ø±Ø­Ù„Ù‡'
                        : 'Ø¨Ø±Ø§ÛŒ Ø±ÙØªÙ† Ø¨Ù‡ Ù…Ø±Ø­Ù„Ù‡ Ø¨Ø¹Ø¯ Ø§Ø² Ø¯Ú©Ù…Ù‡ Ø§Ø¯Ø§Ù…Ù‡ Ø¯Ø§Ø®Ù„ ÙØ±Ù… Ø§Ø³ØªÙØ§Ø¯Ù‡ Ú©Ù†'
                  }
                  className={`flex items-center justify-center gap-2 rounded-2xl border px-3 py-3 text-sm transition ${
                    isActive
                      ? 'cursor-default border-amber-300 bg-amber-400 text-slate-950 shadow-lg shadow-amber-500/20'
                      : done
                        ? 'border-emerald-400/30 bg-emerald-400/10 text-emerald-200 hover:bg-emerald-400/15'
                        : 'cursor-not-allowed border-white/5 bg-slate-950/40 text-slate-600 opacity-60'
                  }`}
                >
                  {item.icon}
                  {item.label}
                </button>
              );
            })}
          </div>
        </section>

        {message && (
          <div className="rounded-2xl border border-red-400/30 bg-red-500/10 px-4 py-3 text-sm text-red-100">{message}</div>
        )}

        {isAuthenticated && step === 'cart' && (
          <div className="flex items-center justify-between rounded-2xl border border-emerald-400/20 bg-emerald-500/10 px-4 py-3 text-sm text-emerald-100">
            <span className="flex items-center gap-2"><ShieldCheck className="h-5 w-5" /> ÙˆØ±ÙˆØ¯ Ø´Ù…Ø§ ØªØ§ÛŒÛŒØ¯ Ø´Ø¯Ù‡ Ø§Ø³Øª.</span>
            <span>{authUser?.phone || authUser?.fullName || 'Ù…Ø´ØªØ±ÛŒ Carrtell'}</span>
          </div>
        )}

        <div className="grid gap-6 lg:grid-cols-[1fr_380px]">
          <section className="space-y-6">
            {step === 'cart' && (
              <div className={cardClass}>
                <div className="mb-4 flex items-center justify-between">
                  <h2 className="text-xl font-black">Ù…Ø­ØµÙˆÙ„Ø§Øª Ø³Ø¨Ø¯ Ø®Ø±ÛŒØ¯</h2>
                  <Link to="/shop" className="text-sm text-amber-300 hover:text-amber-200">Ø§Ø¯Ø§Ù…Ù‡ Ø®Ø±ÛŒØ¯</Link>
                </div>
                {items.length > 0 && (
                  <div className="mb-5 rounded-2xl border border-amber-300/20 bg-amber-400/10 p-4">
                    <div className="mb-2 flex items-center justify-between gap-3 text-sm">
                      <span className="font-bold text-amber-100">
                        {freeShippingRemaining > 0 ? `ÙÙ‚Ø· ${money(freeShippingRemaining)} ØªØ§ Ø§Ø±Ø³Ø§Ù„ Ø±Ø§ÛŒÚ¯Ø§Ù† Ø¨Ø§Ù‚ÛŒ Ù…Ø§Ù†Ø¯Ù‡` : 'Ø§Ø±Ø³Ø§Ù„ Ø±Ø§ÛŒÚ¯Ø§Ù† Ø¨Ø±Ø§ÛŒ Ø§ÛŒÙ† Ø³ÙØ§Ø±Ø´ ÙØ¹Ø§Ù„ Ø´Ø¯'}
                      </span>
                      <Truck className="h-5 w-5 text-amber-300" />
                    </div>
                    <div className="h-2 overflow-hidden rounded-full bg-slate-800">
                      <div className="h-full rounded-full bg-amber-400 transition-all" style={{ width: `${freeShippingProgress}%` }} />
                    </div>
                  </div>
                )}
                {!items.length ? (
                  <div className="rounded-3xl border border-dashed border-white/15 bg-white/[0.03] px-6 py-12 text-center">
                    <div className="mx-auto flex h-16 w-16 items-center justify-center rounded-3xl bg-white/5 text-slate-400">
                      <ShoppingCart className="h-8 w-8" />
                    </div>
                    <h3 className="mt-4 text-lg font-black text-white">Ø³Ø¨Ø¯ Ø®Ø±ÛŒØ¯ Ø´Ù…Ø§ Ø®Ø§Ù„ÛŒ Ø§Ø³Øª</h3>
                    <p className="mx-auto mt-2 max-w-md text-sm leading-7 text-slate-400">Ù…Ø­ØµÙˆÙ„ Ù…ÙˆØ±Ø¯Ù†ÛŒØ§Ø² Ø®ÙˆØ¯Ø±Ùˆ Ø±Ø§ Ø§Ø² ÙØ±ÙˆØ´Ú¯Ø§Ù‡ Ø§Ù†ØªØ®Ø§Ø¨ Ú©Ù†Ø› Ú©Ø§Ù„Ø§Ù‡Ø§ Ù‡Ù…ÛŒÙ†â€ŒØ¬Ø§ Ø¨Ø±Ø§ÛŒ Ø§Ø¯Ø§Ù…Ù‡ Ø³ÙØ§Ø±Ø´ Ù†Ù…Ø§ÛŒØ´ Ø¯Ø§Ø¯Ù‡ Ù…ÛŒâ€ŒØ´ÙˆÙ†Ø¯.</p>
                    <Link to="/" className="mt-5 inline-flex items-center justify-center gap-2 rounded-2xl bg-amber-400 px-6 py-3 font-black text-slate-950 hover:bg-amber-300">
                      Ø±ÙØªÙ† Ø¨Ù‡ ÙØ±ÙˆØ´Ú¯Ø§Ù‡ <ArrowLeft className="h-4 w-4" />
                    </Link>
                  </div>
                ) : (
                  <div className="space-y-3">
                    {items.map((item) => {
                      const id = getItemId(item);
                      return (
                        <div key={id} className="group grid gap-3 rounded-3xl border border-white/10 bg-slate-950/60 p-3.5 transition hover:border-amber-300/25 hover:bg-slate-950/80 sm:grid-cols-[88px_1fr_auto]">
                          <Link to={`/product/${id}`} className="flex h-[88px] w-[88px] items-center justify-center overflow-hidden rounded-2xl border border-white/5 bg-white">
                            {(item.image_url || item.image) ? (
                              <img src={item.image_url || item.image} alt={getItemName(item)} className="h-full w-full object-contain p-1.5 transition duration-300 group-hover:scale-105" />
                            ) : <ShoppingCart className="h-8 w-8 text-slate-300" />}
                          </Link>
                          <div className="min-w-0">
                            <Link to={`/product/${id}`} className="line-clamp-2 font-black leading-7 text-white hover:text-amber-200">{getItemName(item)}</Link>
                            <p className="mt-1 text-xs text-slate-400">{item.brand_name || item.category_name || 'Ù…Ø­ØµÙˆÙ„ Ø®ÙˆØ¯Ø±Ùˆ'}</p>
                            <div className="mt-3 flex flex-wrap items-end gap-x-4 gap-y-1">
                              <p className="text-sm text-slate-400">Ù‚ÛŒÙ…Øª ÙˆØ§Ø­Ø¯: <b className="text-slate-200">{money(getItemPrice(item))}</b></p>
                              <p className="font-black text-amber-300">Ø¬Ù…Ø¹: {money(getItemPrice(item) * getItemQty(item))}</p>
                            </div>
                          </div>
                          <div className="flex items-center justify-between gap-3 sm:flex-col sm:items-end">
                            <button type="button" onClick={() => removeItem(id)} className="inline-flex h-9 items-center gap-1.5 rounded-xl border border-red-400/20 bg-red-500/10 px-3 text-xs font-bold text-red-200 hover:bg-red-500/20">
                              <Trash2 className="h-3.5 w-3.5" /> Ø­Ø°Ù
                            </button>
                            <div className="flex h-10 items-center overflow-hidden rounded-xl border border-white/10 bg-slate-900">
                              <button type="button" onClick={() => updateQty(id, getItemQty(item) + 1)} className="h-full w-10 text-lg font-black hover:bg-white/5">+</button>
                              <span className="w-9 text-center font-black">{getItemQty(item).toLocaleString('fa-IR')}</span>
                              <button type="button" onClick={() => updateQty(id, getItemQty(item) - 1)} className="h-full w-10 text-lg font-black hover:bg-white/5">âˆ’</button>
                            </div>
                          </div>
                        </div>
                      );
                    })}
                  </div>
                )}
                <div className="mt-5 flex justify-end">
                  <button onClick={beginCheckout} className="rounded-2xl bg-amber-400 px-6 py-3 font-bold text-slate-950 hover:bg-amber-300">
                    Ø§Ø¯Ø§Ù…Ù‡ Ø«Ø¨Øª Ø³ÙØ§Ø±Ø´
                  </button>
                </div>
              </div>
            )}

            {step === 'info' && (
              <div className={cardClass}>
                <h2 className="mb-4 text-xl font-black">Ø§Ø·Ù„Ø§Ø¹Ø§Øª Ù…Ø´ØªØ±ÛŒ</h2>
                <div className="grid gap-4 md:grid-cols-2">
                  <label className="space-y-2 text-sm text-slate-300">
                    Ù†Ø§Ù… Ùˆ Ù†Ø§Ù… Ø®Ø§Ù†ÙˆØ§Ø¯Ú¯ÛŒ
                    <input className={inputClass} value={customer.full_name} onChange={(e) => setCustomer({ ...customer, full_name: e.target.value })} placeholder="Ù…Ø«Ù„Ø§Ù‹ Ø§Ù…ÛŒÙ† Ø§ÙˆØ±Ø¹ÛŒ" />
                  </label>
                  <label className="space-y-2 text-sm text-slate-300">
                    Ø´Ù…Ø§Ø±Ù‡ ØªÙ…Ø§Ø³
                    <input inputMode="tel" className={inputClass} value={customer.phone} onChange={(e) => setCustomer({ ...customer, phone: toLocalCheckoutPhone(e.target.value) })} placeholder="09xxxxxxxxx" />
                  </label>
                  <label className="space-y-2 text-sm text-slate-300 md:col-span-2">
                    ØªÙˆØ¶ÛŒØ­Ø§Øª Ø³ÙØ§Ø±Ø´
                    <textarea className={inputClass} rows={4} value={customer.note} onChange={(e) => setCustomer({ ...customer, note: e.target.value })} placeholder="ØªÙˆØ¶ÛŒØ­Ø§Øª Ø§Ø®ØªÛŒØ§Ø±ÛŒ Ø¨Ø±Ø§ÛŒ Ø³ÙØ§Ø±Ø´ ÛŒØ§ Ø³Ø±ÙˆÛŒØ³" />
                  </label>
                </div>
                <div className="mt-5 flex justify-between">
                  <button onClick={() => setStep('cart')} className="rounded-2xl border border-white/10 px-5 py-3 text-slate-200 hover:bg-white/5">Ø¨Ø§Ø²Ú¯Ø´Øª</button>
                  <button disabled={!customerInfoValid} onClick={() => void saveCustomerAndContinue()} className="rounded-2xl bg-amber-400 px-6 py-3 font-bold text-slate-950 hover:bg-amber-300 disabled:cursor-not-allowed disabled:opacity-40">Ø§Ø¯Ø§Ù…Ù‡</button>
                </div>
              </div>
            )}

            {step === 'address' && (
              <div className={cardClass}>
                <h2 className="mb-4 text-xl font-black">Ø¢Ø¯Ø±Ø³ Ùˆ Ø±ÙˆØ´ Ø¯Ø±ÛŒØ§ÙØª</h2>
                <div className="mb-5 grid gap-3 md:grid-cols-2">
                  <button type="button" onClick={() => { setDeliveryMode('delivery'); setServiceDate(''); setServiceTime(''); setAddressFieldErrors({}); setMessage(''); }} className={`rounded-2xl border p-4 text-right ${deliveryMode === 'delivery' ? 'border-amber-300 bg-amber-400/10' : 'border-white/10 bg-slate-950/50'}`}>
                    <Truck className="mb-2 h-5 w-5 text-amber-300" />
                    <b>Ø§Ø±Ø³Ø§Ù„ Ù…Ø­ØµÙˆÙ„</b>
                    <p className="mt-1 text-sm text-slate-400">Ù…Ø­ØµÙˆÙ„ Ø¨Ø±Ø§ÛŒ Ù…Ø´ØªØ±ÛŒ Ø§Ø±Ø³Ø§Ù„ Ø´ÙˆØ¯.</p>
                  </button>
                  <button type="button" onClick={() => { setDeliveryMode('service'); setAddressFieldErrors({}); setMessage(''); }} className={`rounded-2xl border p-4 text-right ${deliveryMode === 'service' ? 'border-amber-300 bg-amber-400/10' : 'border-white/10 bg-slate-950/50'}`}>
                    <Wrench className="mb-2 h-5 w-5 text-amber-300" />
                    <b>Ø®Ø±ÛŒØ¯ + Ø³Ø±ÙˆÛŒØ³ Ø¯Ø± Ù…Ø­Ù„</b>
                    <p className="mt-1 text-sm text-slate-400">Ù…Ø­ØµÙˆÙ„ Ù‡Ù…Ø±Ø§Ù‡ Ø³Ø±ÙˆÛŒØ³ Ø¯Ø± Ù…Ø­Ù„ Ø§Ù†Ø¬Ø§Ù… Ø´ÙˆØ¯.</p><p className="mt-2 text-xs font-bold text-amber-200">Ø§ÛŒØ§Ø¨â€ŒÙˆØ°Ù‡Ø§Ø¨ {money(dispatchFee)} + Ø§Ø¬Ø±Øª {money(serviceLaborFee)}</p>
                  </button>
                </div>

                {deliveryMode === 'service' && (
                  <div className="mb-5 overflow-hidden rounded-3xl border border-red-200 bg-white shadow-sm">
                    <div className="border-b border-red-100 bg-gradient-to-l from-red-50 to-white px-5 py-4">
                      <h3 className="flex items-center gap-2 font-black text-slate-900"><CalendarDays className="h-5 w-5 text-red-600" /> ØªØ§Ø±ÛŒØ® Ùˆ Ø³Ø§Ø¹Øª Ø¯Ø±ÛŒØ§ÙØª Ø®Ø¯Ù…Ø§Øª</h3>
                      <p className="mt-1 text-xs text-slate-500">Ø²Ù…Ø§Ù† Ø§Ù†ØªØ®Ø§Ø¨â€ŒØ´Ø¯Ù‡ Ø¨Ù‡ Ù…Ø¯ÛŒØ± Ùˆ ØªÚ©Ù†Ø³ÛŒÙ† Ù†Ù…Ø§ÛŒØ´ Ø¯Ø§Ø¯Ù‡ Ù…ÛŒâ€ŒØ´ÙˆØ¯.</p>
                    </div>

                    <div className="space-y-6 p-5">
                      <div>
                        <div className="mb-3 flex items-center justify-between gap-3">
                          <span className="text-sm font-black text-slate-800">Û±. Ø±ÙˆØ² Ù…Ø±Ø§Ø¬Ø¹Ù‡ Ø±Ø§ Ø§Ù†ØªØ®Ø§Ø¨ Ú©Ù†</span>
                          {serviceDate && <span className="rounded-full bg-emerald-50 px-3 py-1 text-xs font-bold text-emerald-700">ØªØ§Ø±ÛŒØ® Ø§Ù†ØªØ®Ø§Ø¨ Ø´Ø¯</span>}
                        </div>
                        <div className="grid grid-cols-2 gap-2 sm:grid-cols-3 lg:grid-cols-4">
                          {availableServiceDates.map((date) => (
                            <button
                              key={date.value}
                              type="button"
                              onClick={() => { setServiceDate(date.value); setMessage(''); }}
                              data-booking-slot className={`ct-booking-slot rounded-2xl border px-3 py-3 text-center transition ${serviceDate === date.value ? 'border-red-600 bg-red-600 text-white shadow-md shadow-red-200' : 'border-slate-200 bg-slate-50 text-slate-700 hover:border-red-300 hover:bg-red-50'}`}
                            >
                              <span className="block text-sm font-black">{date.label}</span>
                              {date.isToday && <span className={`mt-1 block text-[11px] ${serviceDate === date.value ? 'text-red-100' : 'text-red-600'}`}>Ø§Ù…Ø±ÙˆØ²</span>}
                            </button>
                          ))}
                        </div>
                      </div>

                      <div>
                        <div className="mb-3 flex items-center gap-2 text-sm font-black text-slate-800"><Clock3 className="h-4 w-4 text-red-600" /> Û². Ø¨Ø§Ø²Ù‡ Ø³Ø§Ø¹Øª Ù…Ø±Ø§Ø¬Ø¹Ù‡ Ø±Ø§ Ø§Ù†ØªØ®Ø§Ø¨ Ú©Ù†</div>
                        <div className="grid grid-cols-2 gap-2 sm:grid-cols-3">
                          {SERVICE_TIME_SLOTS.map((slot) => (
                            <button
                              key={slot.value}
                              type="button"
                              onClick={() => { setServiceTime(slot.value); setMessage(''); }}
                              data-booking-slot className={`ct-booking-slot rounded-2xl border px-3 py-3 text-sm font-black transition ${serviceTime === slot.value ? 'border-red-600 bg-red-600 text-white shadow-md shadow-red-200' : 'border-slate-200 bg-white text-slate-700 hover:border-red-300 hover:bg-red-50'}`}
                            >
                              {slot.label}
                            </button>
                          ))}
                        </div>
                      </div>

                      {!serviceScheduleReady && <p className="rounded-2xl bg-amber-50 px-4 py-3 text-xs font-bold text-amber-700">Ø¨Ø±Ø§ÛŒ Ø§Ø¯Ø§Ù…Ù‡ØŒ ÛŒÚ© Ø±ÙˆØ² Ùˆ ÛŒÚ© Ø¨Ø§Ø²Ù‡ Ø²Ù…Ø§Ù†ÛŒ Ø§Ù†ØªØ®Ø§Ø¨ Ú©Ù†.</p>}
                    </div>
                  </div>
                )}

                {deliveryMode === 'delivery' && (
                  <div className="mb-5 rounded-2xl border border-white/10 bg-slate-950/50 p-4">
                    <div className="mb-3 flex items-center justify-between gap-3">
                      <h3 className="font-black text-white">Ø±ÙˆØ´ Ø§Ø±Ø³Ø§Ù„</h3>
                      <span className="text-xs text-slate-400">Ù‡Ø²ÛŒÙ†Ù‡ Ø¨Ù‡â€ŒØµÙˆØ±Øª Ù„Ø­Ø¸Ù‡â€ŒØ§ÛŒ Ù…Ø­Ø§Ø³Ø¨Ù‡ Ù…ÛŒâ€ŒØ´ÙˆØ¯</span>
                    </div>
                    <div className="grid gap-3 md:grid-cols-3">
                      {[
                        { key: 'standard', title: 'Ø§Ø±Ø³Ø§Ù„ Ø¹Ø§Ø¯ÛŒ', desc: 'ØªØ­ÙˆÛŒÙ„ Û² ØªØ§ Û´ Ø±ÙˆØ² Ú©Ø§Ø±ÛŒ', fee: subtotal >= freeShippingThreshold ? 'Ø±Ø§ÛŒÚ¯Ø§Ù†' : money(120000) },
                        { key: 'express', title: 'Ø§Ø±Ø³Ø§Ù„ Ø³Ø±ÛŒØ¹', desc: 'ØªØ­ÙˆÛŒÙ„ Ø³Ø±ÛŒØ¹â€ŒØªØ±', fee: money(220000) },
                        { key: 'pickup', title: 'ØªØ­ÙˆÛŒÙ„ Ø­Ø¶ÙˆØ±ÛŒ', desc: 'Ø¯Ø±ÛŒØ§ÙØª Ø§Ø² Ø´Ø¹Ø¨Ù‡', fee: 'Ø±Ø§ÛŒÚ¯Ø§Ù†' },
                      ].map((method) => (
                        <button
                          key={method.key}
                          type="button"
                          onClick={() => { setShippingMethod(method.key as 'standard' | 'express' | 'pickup'); setAddressFieldErrors({}); }}
                          className={`rounded-2xl border p-3 text-right transition ${shippingMethod === method.key ? 'border-amber-300 bg-amber-400/10' : 'border-white/10 bg-slate-900/70 hover:border-amber-300/30'}`}
                        >
                          <b className="block text-white">{method.title}</b>
                          <span className="mt-1 block text-xs text-slate-400">{method.desc}</span>
                          <span className="mt-2 block text-xs font-black text-amber-200">{method.fee}</span>
                        </button>
                      ))}
                    </div>
                  </div>
                )}

                {shippingMethod !== 'pickup' && <div className="space-y-4">
                  {addresses.length > 0 && (
                    <div className="space-y-3">
                      <div className="flex flex-wrap items-center justify-between gap-3">
                        <h3 className="font-bold text-white">Ø¢Ø¯Ø±Ø³â€ŒÙ‡Ø§ÛŒ Ø°Ø®ÛŒØ±Ù‡â€ŒØ´Ø¯Ù‡</h3>
                        <button
                          type="button"
                          onClick={() => {
                            setSelectedAddressId('');
                            setShowNewAddressForm(true);
                            setAddressFieldErrors({});
                            setMessage('');
                          }}
                          className="rounded-xl border border-amber-300/40 bg-amber-400/10 px-4 py-2 text-sm font-bold text-amber-200 hover:bg-amber-400/20"
                        >
                          + Ø§ÙØ²ÙˆØ¯Ù† Ø¢Ø¯Ø±Ø³
                        </button>
                      </div>
                      <div className="grid gap-3 md:grid-cols-2">
                        {addresses.map((address) => {
                          const active = selectedAddressId === String(address.id) && !showNewAddressForm;
                          return (
                            <button
                              key={address.id}
                              type="button"
                              onClick={() => {
                                setSelectedAddressId(String(address.id));
                                setShowNewAddressForm(false);
                                setAddressFieldErrors({});
                                setMessage('');
                              }}
                              className={`rounded-2xl border p-4 text-right transition ${active ? 'border-emerald-400 bg-emerald-400/10' : 'border-white/10 bg-slate-950/50 hover:border-amber-300/40'}`}
                            >
                              <div className="font-bold text-white">{address.title || 'Ø¢Ø¯Ø±Ø³ Ø°Ø®ÛŒØ±Ù‡â€ŒØ´Ø¯Ù‡'}</div>
                              <div className="mt-2 text-sm leading-6 text-slate-400">{addressText(address)}</div>
                            </button>
                          );
                        })}
                      </div>
                    </div>
                  )}

                  {addresses.length === 0 && !showNewAddressForm && (
                    <button type="button" onClick={() => setShowNewAddressForm(true)} className="rounded-2xl bg-amber-400 px-5 py-3 font-bold text-slate-950">Ø§ÙØ²ÙˆØ¯Ù† Ø¢Ø¯Ø±Ø³</button>
                  )}

                  {showNewAddressForm && (
                    <div className="grid gap-4 md:grid-cols-2">
                      <label className="space-y-2 text-sm text-slate-300">
                        Ø§Ø³ØªØ§Ù†
                        <select
                          className={`${selectClass} ${addressFieldErrors.province ? 'border-red-400 focus:border-red-400 focus:ring-red-400/20' : ''}`}
                          value={newAddress.province}
                          onChange={(e) => {
                            setNewAddress({ ...newAddress, province: e.target.value, county: '' });
                            setAddressFieldErrors((prev) => ({ ...prev, province: undefined, county: undefined }));
                          }}
                        >
                          <option value="">Ø§Ù†ØªØ®Ø§Ø¨ Ø§Ø³ØªØ§Ù†</option>
                          {IRAN_PROVINCES.map((province) => <option key={province.name} value={province.name}>{province.name}</option>)}
                        </select>
                        {addressFieldErrors.province && <span className="block text-xs text-red-300">{addressFieldErrors.province}</span>}
                      </label>
                      <label className="space-y-2 text-sm text-slate-300">
                        Ø´Ù‡Ø±Ø³ØªØ§Ù†
                        <select
                          className={`${selectClass} ${addressFieldErrors.county ? 'border-red-400 focus:border-red-400 focus:ring-red-400/20' : ''}`}
                          value={newAddress.county}
                          disabled={!newAddress.province}
                          onChange={(e) => {
                            setNewAddress({ ...newAddress, county: e.target.value });
                            setAddressFieldErrors((prev) => ({ ...prev, county: undefined }));
                          }}
                        >
                          <option value="">Ø§Ù†ØªØ®Ø§Ø¨ Ø´Ù‡Ø±Ø³ØªØ§Ù†</option>
                          {getProvinceCounties(newAddress.province).map((county) => <option key={county} value={county}>{county}</option>)}
                        </select>
                        {addressFieldErrors.county && <span className="block text-xs text-red-300">{addressFieldErrors.county}</span>}
                      </label>
                      <label className="space-y-2 text-sm text-slate-300">
                        Ù…Ø­Ù„Ù‡
                        <input className={inputClass} placeholder="Ù†Ø§Ù… Ù…Ø­Ù„Ù‡" value={newAddress.neighborhood} onChange={(e) => setNewAddress({ ...newAddress, neighborhood: e.target.value })} />
                      </label>
                      <label className="space-y-2 text-sm text-slate-300 md:col-span-2">
                        Ø¢Ø¯Ø±Ø³ Ø¯Ù‚ÛŒÙ‚
                        <textarea
                          className={`${inputClass} ${addressFieldErrors.street ? 'border-red-400 focus:border-red-400 focus:ring-red-400/20' : ''}`}
                          rows={3}
                          placeholder="Ø®ÛŒØ§Ø¨Ø§Ù†ØŒ Ú©ÙˆÚ†Ù‡ Ùˆ Ù†Ø´Ø§Ù†ÛŒ Ø¯Ù‚ÛŒÙ‚"
                          value={newAddress.street}
                          onChange={(e) => {
                            setNewAddress({ ...newAddress, street: e.target.value });
                            setAddressFieldErrors((prev) => ({ ...prev, street: undefined }));
                          }}
                        />
                        {addressFieldErrors.street && <span className="block text-xs text-red-300">{addressFieldErrors.street}</span>}
                      </label>
                      <div className="md:col-span-2">
                        <MapLocationPicker
                          initialLatitude={newAddress.latitude ? Number(newAddress.latitude) : null}
                          initialLongitude={newAddress.longitude ? Number(newAddress.longitude) : null}
                          onConfirm={(location) => setNewAddress((prev) => ({ ...prev, latitude: String(location.latitude), longitude: String(location.longitude) }))}
                        />
                      </div>
                      <div className="md:col-span-2 rounded-2xl border border-white/10 bg-slate-950/50 p-4">
                        <label className="flex cursor-pointer items-center gap-3 text-sm font-bold text-white">
                          <input type="checkbox" checked={saveAddressForLater} onChange={(e) => setSaveAddressForLater(e.target.checked)} className="h-5 w-5 accent-amber-400" />
                          Ø§ÛŒÙ† Ø¢Ø¯Ø±Ø³ Ø¨Ø±Ø§ÛŒ Ø®Ø±ÛŒØ¯Ù‡Ø§ÛŒ Ø¨Ø¹Ø¯ÛŒ Ø¯Ø± Ù¾Ø±ÙˆÙØ§ÛŒÙ„ Ù…Ù† Ø°Ø®ÛŒØ±Ù‡ Ø´ÙˆØ¯
                        </label>
                        {saveAddressForLater && (
                          <label className="mt-4 block space-y-2 text-sm text-slate-300">
                            Ù†Ø§Ù… Ø¢Ø¯Ø±Ø³
                            <input className={inputClass} value={addressTitle} onChange={(e) => setAddressTitle(e.target.value)} placeholder="Ù…Ø«Ù„Ø§Ù‹ Ø®Ø§Ù†Ù‡ØŒ Ù…Ø­Ù„ Ú©Ø§Ø± ÛŒØ§ Ù…ØºØ§Ø²Ù‡" />
                          </label>
                        )}
                      </div>
                    </div>
                  )}
                </div>}

                {shippingMethod === 'pickup' && deliveryMode === 'delivery' && (
                  <div className="rounded-2xl border border-emerald-400/20 bg-emerald-500/10 p-4 text-sm leading-7 text-emerald-100">
                    Ø³ÙØ§Ø±Ø´ Ø¨Ù‡â€ŒØµÙˆØ±Øª Ø­Ø¶ÙˆØ±ÛŒ ØªØ­ÙˆÛŒÙ„ Ù…ÛŒâ€ŒØ´ÙˆØ¯ Ùˆ Ù†ÛŒØ§Ø²ÛŒ Ø¨Ù‡ Ø«Ø¨Øª Ø¢Ø¯Ø±Ø³ ÛŒØ§ Ú©Ø¯Ù¾Ø³ØªÛŒ Ù†ÛŒØ³Øª. Ø²Ù…Ø§Ù† Ø¢Ù…Ø§Ø¯Ù‡â€ŒØ³Ø§Ø²ÛŒ Ù¾Ø³ Ø§Ø² Ø«Ø¨Øª Ø³ÙØ§Ø±Ø´ Ø§Ø·Ù„Ø§Ø¹â€ŒØ±Ø³Ø§Ù†ÛŒ Ù…ÛŒâ€ŒØ´ÙˆØ¯.
                  </div>
                )}

                <div className="mt-5 flex justify-between">
                  <button onClick={() => setStep('info')} className="rounded-2xl border border-white/10 px-5 py-3 text-slate-200 hover:bg-white/5">Ø¨Ø§Ø²Ú¯Ø´Øª</button>
                  <button disabled={savingAddress || !addressReady || !serviceScheduleReady} onClick={() => void goToPayment()} className="rounded-2xl bg-amber-400 px-6 py-3 font-bold text-slate-950 hover:bg-amber-300 disabled:cursor-not-allowed disabled:opacity-40">{savingAddress ? 'Ø¯Ø± Ø­Ø§Ù„ Ø°Ø®ÛŒØ±Ù‡...' : 'Ø§Ø¯Ø§Ù…Ù‡ Ù¾Ø±Ø¯Ø§Ø®Øª'}</button>
                </div>
              </div>
            )}

            {step === 'payment' && (
              <div className={cardClass}>
                <h2 className="mb-4 text-xl font-black">Ø¨Ø§Ø²Ø¨ÛŒÙ†ÛŒ Ùˆ Ù¾Ø±Ø¯Ø§Ø®Øª</h2>
                <div className="space-y-3 rounded-2xl border border-white/10 bg-slate-950/60 p-4 text-sm text-slate-300">
                  <p>Ù…Ø´ØªØ±ÛŒ: <b className="text-white">{customer.full_name}</b></p>
                  <p>Ø´Ù…Ø§Ø±Ù‡ ØªÙ…Ø§Ø³: <b className="text-white">{customer.phone}</b></p>
                  <p>Ø±ÙˆØ´ Ø¯Ø±ÛŒØ§ÙØª: <b className="text-white">{deliveryMode === 'service' ? 'Ø®Ø±ÛŒØ¯ + Ø³Ø±ÙˆÛŒØ³ Ø¯Ø± Ù…Ø­Ù„' : shippingMethod === 'pickup' ? 'ØªØ­ÙˆÛŒÙ„ Ø­Ø¶ÙˆØ±ÛŒ' : shippingMethod === 'express' ? 'Ø§Ø±Ø³Ø§Ù„ Ø³Ø±ÛŒØ¹' : 'Ø§Ø±Ø³Ø§Ù„ Ø¹Ø§Ø¯ÛŒ'}</b></p>
                  <p>Ø¢Ø¯Ø±Ø³: <b className="text-white">{shippingMethod === 'pickup' ? 'ØªØ­ÙˆÛŒÙ„ Ø­Ø¶ÙˆØ±ÛŒ Ø§Ø² Ø´Ø¹Ø¨Ù‡' : selectedAddress && !showNewAddressForm ? addressText(selectedAddress) : [newAddress.province, newAddress.county, newAddress.neighborhood, newAddress.street].filter(Boolean).join('ØŒ ')}</b></p>
                  {deliveryMode === 'service' && <p>Ø²Ù…Ø§Ù† Ø³Ø±ÙˆÛŒØ³: <b className="text-white">{serviceDate ? new Intl.DateTimeFormat('fa-IR-u-ca-persian', { dateStyle: 'full' }).format(new Date(`${serviceDate}T12:00:00`)) : '-'}ØŒ Ø¨Ø§Ø²Ù‡ {SERVICE_TIME_SLOTS.find((slot) => slot.value === serviceTime)?.label || '-'}</b></p>}
                  {carTitle && <p>Ø®ÙˆØ¯Ø±Ùˆ: <b className="text-amber-200">{carTitle}</b></p>}
                </div>
                <div className="mt-5 flex justify-between">
                  <button onClick={() => setStep('address')} className="rounded-2xl border border-white/10 px-5 py-3 text-slate-200 hover:bg-white/5">Ø¨Ø§Ø²Ú¯Ø´Øª</button>
                  <button disabled={loading} onClick={createOrder} className="rounded-2xl bg-emerald-400 px-6 py-3 font-bold text-slate-950 hover:bg-emerald-300 disabled:opacity-50">
                    {loading ? 'Ø¯Ø± Ø­Ø§Ù„ Ø«Ø¨Øª...' : 'Ø«Ø¨Øª Ø³ÙØ§Ø±Ø´ Ùˆ Ø±ÙØªÙ† Ø¨Ù‡ Ù¾Ø±Ø¯Ø§Ø®Øª'}
                  </button>
                </div>
              </div>
            )}

            {step === 'done' && (
              <div className={cardClass}>
                <CheckCircle2 className="mb-3 h-10 w-10 text-emerald-300" />
                <h2 className="text-xl font-black">Ø³ÙØ§Ø±Ø´ Ø«Ø¨Øª Ø´Ø¯</h2>
                <p className="mt-2 text-slate-300">Ø¨Ø±Ø§ÛŒ Ù¾Ø±Ø¯Ø§Ø®Øª Ø¨Ù‡ ØµÙØ­Ù‡ Ù¾Ø±Ø¯Ø§Ø®Øª Ù…Ù†ØªÙ‚Ù„ Ù…ÛŒâ€ŒØ´ÙˆÛŒ.</p>
                {createdOrderId && <p className="mt-2 text-sm text-slate-400">Ú©Ø¯ Ø³ÙØ§Ø±Ø´: {createdOrderId}</p>}
              </div>
            )}
          </section>

          <aside className="space-y-6 lg:sticky lg:top-24 lg:self-start">
            <div className={cardClass}>
              <h3 className="mb-4 text-lg font-black">Ø®Ù„Ø§ØµÙ‡ Ø³ÙØ§Ø±Ø´</h3>
              <div className="space-y-3 text-sm text-slate-300">
                <div className="flex justify-between"><span>ØªØ¹Ø¯Ø§Ø¯ Ú©Ø§Ù„Ø§</span><b>{items.length.toLocaleString('fa-IR')}</b></div>
                <div className="flex justify-between"><span>Ø¬Ù…Ø¹ Ú©Ø§Ù„Ø§Ù‡Ø§</span><b>{money(subtotal)}</b></div>
                <div className="flex justify-between"><span>Ø±ÙˆØ´ Ø¯Ø±ÛŒØ§ÙØª</span><b>{deliveryMode === 'service' ? 'Ø³Ø±ÙˆÛŒØ³ Ø¯Ø± Ù…Ø­Ù„' : shippingMethod === 'pickup' ? 'ØªØ­ÙˆÛŒÙ„ Ø­Ø¶ÙˆØ±ÛŒ' : shippingMethod === 'express' ? 'Ø§Ø±Ø³Ø§Ù„ Ø³Ø±ÛŒØ¹' : 'Ø§Ø±Ø³Ø§Ù„ Ø¹Ø§Ø¯ÛŒ'}</b></div>
                {deliveryMode === 'delivery' && <div className="flex justify-between"><span>Ù‡Ø²ÛŒÙ†Ù‡ Ø§Ø±Ø³Ø§Ù„</span><b>{shippingFee === 0 ? 'Ø±Ø§ÛŒÚ¯Ø§Ù†' : money(shippingFee)}</b></div>}
                {deliveryMode === 'service' && (
                  <>
                    <div className="flex justify-between"><span>Ù‡Ø²ÛŒÙ†Ù‡ Ø§ÛŒØ§Ø¨ Ùˆ Ø°Ù‡Ø§Ø¨</span><b>{money(dispatchFee)}</b></div>
                    <div className="flex justify-between"><span>Ø§Ø¬Ø±Øª Ø³Ø±ÙˆÛŒØ³ Ø¯Ø± Ù…Ø­Ù„</span><b>{money(serviceLaborFee)}</b></div>
                  </>
                )}
                {discountAmount > 0 && <div className="flex justify-between text-emerald-300"><span>ØªØ®ÙÛŒÙ ({appliedDiscount?.code})</span><b>- {money(discountAmount)}</b></div>}
                <div className="border-t border-white/10 pt-3 text-base">
                  <div className="flex justify-between"><span>Ù…Ø¨Ù„Øº Ù‚Ø§Ø¨Ù„ Ù¾Ø±Ø¯Ø§Ø®Øª</span><b className="text-amber-300">{money(total)}</b></div>
                </div>
              </div>
              {items.length > 0 && step !== 'done' && (
                <div className="mt-4">
                  <DiscountCodeBox
                    total={subtotal}
                    userId={authUser?.id || null}
                    onApplied={(result) => setAppliedDiscount(result)}
                  />
                  {appliedDiscount?.ok && (
                    <button type="button" onClick={() => setAppliedDiscount(null)} className="mt-2 text-xs text-rose-300 hover:text-rose-200">Ø­Ø°Ù Ú©Ø¯ ØªØ®ÙÛŒÙ</button>
                  )}
                </div>
              )}
              {step === 'cart' && items.length > 0 && (
                <button type="button" onClick={beginCheckout} className="mt-4 inline-flex w-full items-center justify-center gap-2 rounded-2xl bg-amber-400 px-5 py-3.5 font-black text-slate-950 shadow-lg shadow-amber-500/10 hover:bg-amber-300">
                  Ø§Ø¯Ø§Ù…Ù‡ Ø«Ø¨Øª Ø³ÙØ§Ø±Ø´ <ArrowLeft className="h-4 w-4" />
                </button>
              )}
            </div>

          </aside>
        </div>
      </div>
    </main>
  );
}

